import { FC } from 'react';
import { Text, View } from 'react-native';

interface Props {}

const Upload: FC<Props> = () => {
  return (
    <View>
      <Text>Upload</Text>
    </View>
  );
};

export default Upload;
