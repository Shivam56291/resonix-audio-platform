import { FC } from 'react';
import { Text, View } from 'react-native';

interface Props {}

const Profile: FC<Props> = () => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};

export default Profile;
